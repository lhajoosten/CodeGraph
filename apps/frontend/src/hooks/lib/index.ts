/**
 * Hook creation utilities and type helpers.
 * These provide a consistent interface for wrapping generated API operations.
 */

export { createQueryHook, type QueryData, type QueryError } from './create-hooks';
